import Foundation
import Jump

enum CoordiantorPaths: ContextPath {
    case viewA(ViewAActions = .idle)
    case viewB(ViewBActions = .idle)
    case viewC(ViewCActions = .idle)
    case viewD(ViewDActions = .idle)
    case viewE(ViewEActions = .idle)
    case viewF(ViewFActions = .idle)
    case viewG(ViewGActions = .idle)
}
